name: Release

'on':
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to build (e.g. v1.2.3). Must already exist or be created by this run.'
        required: true
        type: string

# Least-privilege default; jobs elevate only what they need.
permissions:
  contents: read

# Serialize release runs per ref, but NEVER cancel an in-flight release.
concurrency:
  group: release-${{ github.workflow }}-${{ github.event.inputs.tag || github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: build (${{ matrix.target }})
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        # Native builds only (no cross-compilation of the C SQLite + rustls
        # crypto stack) — each target == its host runner. Linux arm64 uses the
        # free public-repo `ubuntu-24.04-arm` hosted runner; macOS covers both
        # Apple Silicon (macos-14) and Intel (macos-15-intel, since macos-13
        # was retired).
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
          - os: macos-14
            target: aarch64-apple-darwin
          - os: macos-15-intel
            target: x86_64-apple-darwin
    runs-on: ${{ matrix.os }}
    steps:
      # Resolve the tag BEFORE checkout so the workflow_dispatch path can check
      # out the exact tag's tree. Untrusted input is passed via an env var and
      # only ever referenced as a shell variable (never interpolated into the
      # script text), which prevents shell injection.
      - name: Resolve release tag
        id: tag
        shell: bash
        env:
          INPUT_TAG: ${{ github.event.inputs.tag }}
        run: |
          set -euo pipefail
          if [ -n "${INPUT_TAG:-}" ]; then
            TAG="$INPUT_TAG"
          else
            TAG="${GITHUB_REF_NAME:-}"
          fi
          if [ -z "$TAG" ]; then
            echo "::error::Could not determine release tag."
            exit 1
          fi
          if ! [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]; then
            echo "::error::Refusing to build untrusted/invalid tag '$TAG'. Expected vMAJOR.MINOR.PATCH[-suffix]."
            exit 1
          fi
          VERSION="${TAG#v}"
          {
            echo "tag=${TAG}"
            echo "version=${VERSION}"
          } >> "$GITHUB_OUTPUT"
          echo "Resolved tag=${TAG} version=${VERSION}"

      - name: Checkout
        uses: actions/checkout@v7
        with:
          # For tag pushes, GITHUB_REF_NAME already equals the tag, so this is a
          # no-op. For workflow_dispatch, this checks out the dispatched tag's
          # tree (if it exists) instead of the launch ref, ensuring the built
          # binary and Cargo.toml correspond to the tag being released.
          ref: ${{ steps.tag.outputs.tag }}

      - name: Verify tag version matches Cargo.toml
        shell: bash
        env:
          TAG_VERSION: ${{ steps.tag.outputs.version }}
        run: |
          set -euo pipefail
          # Robust extraction: take the first `version = "..."` line that appears
          # after the [package] header, and read the value from inside the quotes
          # so inline comments cannot corrupt it.
          CARGO_VERSION="$(
            awk '
              /^[[:space:]]*\[/ { in_pkg = ($0 ~ /^[[:space:]]*\[package\][[:space:]]*$/) }
              in_pkg && /^[[:space:]]*version[[:space:]]*=/ {
                if (match($0, /"[^"]+"/)) {
                  print substr($0, RSTART + 1, RLENGTH - 2)
                  exit
                }
              }
            ' Cargo.toml
          )"
          echo "Cargo.toml version: ${CARGO_VERSION}"
          echo "Tag version:        ${TAG_VERSION}"
          if [ -z "$CARGO_VERSION" ]; then
            echo "::error::Could not parse version from Cargo.toml [package] section."
            exit 1
          fi
          if [ "${CARGO_VERSION}" != "${TAG_VERSION}" ]; then
            echo "::error::Tag version '${TAG_VERSION}' does not match Cargo.toml version '${CARGO_VERSION}'. Bump Cargo.toml before tagging."
            exit 1
          fi

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build release binary
        shell: bash
        run: cargo build --release --locked --target ${{ matrix.target }}

      - name: Package
        id: package
        shell: bash
        env:
          VERSION: ${{ steps.tag.outputs.version }}
          TARGET: ${{ matrix.target }}
        run: |
          set -euo pipefail
          BIN="matrix-mcp"
          STAGE="$(mktemp -d)"
          ARCHIVE="matrix-mcp-${VERSION}-${TARGET}.tar.gz"

          cp "target/${TARGET}/release/${BIN}" "${STAGE}/${BIN}"
          if [ -f README.md ]; then cp README.md "${STAGE}/README.md"; fi
          if [ -f LICENSE ]; then cp LICENSE "${STAGE}/LICENSE"; fi

          tar -czf "${ARCHIVE}" -C "${STAGE}" .

          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "${ARCHIVE}" > "${ARCHIVE}.sha256"
          else
            shasum -a 256 "${ARCHIVE}" > "${ARCHIVE}.sha256"
          fi

          {
            echo "archive=${ARCHIVE}"
            echo "checksum=${ARCHIVE}.sha256"
          } >> "$GITHUB_OUTPUT"

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: release-${{ matrix.target }}
          path: |
            matrix-mcp-${{ steps.tag.outputs.version }}-${{ matrix.target }}.tar.gz
            matrix-mcp-${{ steps.tag.outputs.version }}-${{ matrix.target }}.tar.gz.sha256
          if-no-files-found: error
          retention-days: 7

  package-mcpb:
    name: package desktop extension (.mcpb)
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Resolve release tag
        id: tag
        shell: bash
        env:
          INPUT_TAG: ${{ github.event.inputs.tag }}
        run: |
          set -euo pipefail
          if [ -n "${INPUT_TAG:-}" ]; then
            TAG="$INPUT_TAG"
          else
            TAG="${GITHUB_REF_NAME:-}"
          fi
          if [ -z "$TAG" ]; then
            echo "::error::Could not determine release tag."
            exit 1
          fi
          if ! [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]; then
            echo "::error::Refusing to package untrusted/invalid tag '$TAG'. Expected vMAJOR.MINOR.PATCH[-suffix]."
            exit 1
          fi
          VERSION="${TAG#v}"
          {
            echo "tag=${TAG}"
            echo "version=${VERSION}"
          } >> "$GITHUB_OUTPUT"

      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: ${{ steps.tag.outputs.tag }}

      - name: Download build artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true

      - name: Extract per-platform binaries
        shell: bash
        env:
          VERSION: ${{ steps.tag.outputs.version }}
        run: |
          set -euo pipefail
          mkdir -p bin
          extract() {
            tar -xzf "dist/matrix-mcp-${VERSION}-${1}.tar.gz" -O ./matrix-mcp > "bin/matrix-mcp-${2}"
            chmod +x "bin/matrix-mcp-${2}"
          }
          extract aarch64-apple-darwin      darwin-arm64
          extract x86_64-apple-darwin       darwin-x64
          extract aarch64-unknown-linux-gnu linux-arm64
          extract x86_64-unknown-linux-gnu  linux-x64

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: '22'

      - name: Build .mcpb bundles
        shell: bash
        run: packaging/mcpb/build.sh "${{ steps.tag.outputs.version }}" bin dist-mcpb

      - name: Upload .mcpb artifacts
        uses: actions/upload-artifact@v7
        with:
          name: mcpb-bundles
          path: dist-mcpb/*.mcpb
          if-no-files-found: error
          retention-days: 7

  release:
    name: publish github release
    needs: [build, package-mcpb]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Resolve release tag
        id: tag
        shell: bash
        env:
          INPUT_TAG: ${{ github.event.inputs.tag }}
        run: |
          set -euo pipefail
          if [ -n "${INPUT_TAG:-}" ]; then
            TAG="$INPUT_TAG"
          else
            TAG="${GITHUB_REF_NAME:-}"
          fi
          if [ -z "$TAG" ]; then
            echo "::error::Could not determine release tag."
            exit 1
          fi
          if ! [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]; then
            echo "::error::Refusing to release untrusted/invalid tag '$TAG'. Expected vMAJOR.MINOR.PATCH[-suffix]."
            exit 1
          fi
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          # A tag containing '-' (e.g. v1.2.3-rc1) is treated as a prerelease.
          if [[ "$TAG" == *-* ]]; then
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
          else
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true

      - name: List downloaded artifacts
        shell: bash
        run: ls -la dist

      - name: Verify artifact checksums
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob
          cd dist
          checksums=( *.sha256 )
          if [ "${#checksums[@]}" -eq 0 ]; then
            echo "::error::No .sha256 files found; cannot verify artifacts."
            exit 1
          fi
          for sum in "${checksums[@]}"; do
            echo "Verifying ${sum}"
            sha256sum -c "${sum}"
          done
          echo "All artifact checksums verified."

      # GitHub's immutable-releases feature locks a release's assets the
      # moment it's published, so uploading files directly to a published
      # release fails ("Cannot upload asset ... to an immutable release").
      # Assets can still be attached to a draft, so: create as a draft with
      # the assets attached, then flip it to published in a separate step
      # that touches nothing else (no generate_release_notes here, or the
      # notes would be regenerated and appended a second time).
      - name: Create draft release with assets
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          generate_release_notes: true
          draft: true
          prerelease: ${{ steps.tag.outputs.prerelease == 'true' }}
          files: |
            dist/*.tar.gz
            dist/*.tar.gz.sha256
            dist/*.mcpb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          draft: false
          prerelease: ${{ steps.tag.outputs.prerelease == 'true' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-crate:
    name: publish to crates.io
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
    steps:
      - name: Report secret availability
        shell: bash
        run: |
          if [ -n "${CARGO_REGISTRY_TOKEN}" ]; then
            echo "CARGO_REGISTRY_TOKEN is set; crate will be published."
          else
            echo "CARGO_REGISTRY_TOKEN is not set; skipping crate publish (no-op)."
          fi

      - name: Checkout
        if: env.CARGO_REGISTRY_TOKEN != ''
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Install Rust (stable)
        if: env.CARGO_REGISTRY_TOKEN != ''
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        if: env.CARGO_REGISTRY_TOKEN != ''
        uses: Swatinem/rust-cache@v2

      - name: Publish crate
        if: env.CARGO_REGISTRY_TOKEN != ''
        shell: bash
        run: |
          set -euo pipefail
          # cargo publish is NOT idempotent: re-running a release for an
          # already-published version returns "crate version already exists".
          # Tolerate exactly that case so re-triggered releases do not fail,
          # while still surfacing any other publish error.
          set +e
          OUTPUT="$(cargo publish --locked 2>&1)"
          STATUS=$?
          set -e
          echo "${OUTPUT}"
          if [ "${STATUS}" -ne 0 ]; then
            if echo "${OUTPUT}" | grep -qiE 'already (exists|uploaded)|crate version .* is already'; then
              echo "::notice::This crate version is already published on crates.io; treating as success."
              exit 0
            fi
            exit "${STATUS}"
          fi
